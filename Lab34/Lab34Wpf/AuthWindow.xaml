<Window x:Class="Lab34Wpf.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация"
        Height="260" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0B1220"
        Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Логин *" Foreground="#94A3B8" Margin="0,0,0,4" />
        <TextBox x:Name="LoginBox" Grid.Row="1" MaxLength="50" Padding="6" />

        <TextBlock Grid.Row="2" Text="Пароль *" Foreground="#94A3B8" Margin="0,12,0,4" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" MaxLength="50" Padding="6" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,18,0,0">
            <Button x:Name="OkButton" Content="Войти" Width="120" Height="32" Margin="0,0,10,0" Click="Ok_Click" />
            <Button Content="Очистить" Width="120" Height="32" Click="Clear_Click" />
        </StackPanel>
    </Grid>
</Window>
