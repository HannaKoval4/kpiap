<Window x:Class="Lab30Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab30Wpf"
        mc:Ignorable="d"
        Title="Овальное окно" Height="250" Width="360"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent"
        Loaded="Window_Loaded" SizeChanged="Window_SizeChanged">
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Border x:Name="newBorder" Opacity="0.9" Background="LightBlue">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="{TemplateBinding Title}" FontWeight="Bold" HorizontalAlignment="Center"
                               Margin="0,10,0,10"
                               MouseLeftButtonDown="TextBlock_MouseLeftButtonDown"/>

                    <Border Grid.Row="1" Margin="16,0,16,0" Background="Transparent">
                        <AdornerDecorator>
                            <ContentPresenter />
                        </AdornerDecorator>
                    </Border>

                    <ResizeGrip Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                Visibility="Collapsed" IsTabStop="False" />
                </Grid>
            </Border>
        </ControlTemplate>
    </Window.Template>

    <Grid>
        <Button x:Name="closeButton" Content="Закрыть" Click="closeButton_Click"
                Width="110" Height="34" Background="LightPink" />
    </Grid>
</Window>
